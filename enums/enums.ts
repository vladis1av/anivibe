export enum LinksPaths {
  anime = '/anime',
}

export enum FyleTypes {
  jpg = 'jpg',
  webp = 'webp',
  torrent = 'torrent',
}

export enum Themes {
  dark = 'dark',
  light = 'light',
}

export enum Colors {
  white = '#FFFFFF',
  brightBlue = '#2299DD',
}
