import { makeStyles } from '@mui/styles';

const useLoadingStatusStyles = makeStyles(() => ({
  loadingInfo: {
    width: '100%',
    height: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));

export default useLoadingStatusStyles;
