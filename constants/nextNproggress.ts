import { Colors } from '@enums/enums';

export const nextNProgressColor: string = Colors.brightBlue;
export const nextNProgressStartPosition: number = 0.3;
export const nextNProgressStopDelayMs: number = 200;
export const nextNProgressHeight: number = 3;